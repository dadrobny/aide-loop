# Concepts

> **STUB — see `/HANDOFF.md` task F.**

TODO: the three layers (engine / adapter / project config), the seven-step loop,
the five role tiers, the merge policy. Distil from `/README.md`,
`core/conventions.md`, and `adapters/ADAPTER-SPEC.md`.
