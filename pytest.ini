[pytest]
# The framework's own tests live in three places: the engine suite under core/,
# the adapter/installer conformance suite under adapters/, and the repo-level
# installer/versioning suite under tests/. A bare `pytest` runs all three.
testpaths = core adapters tests
