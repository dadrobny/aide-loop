[pytest]
# The framework's own tests live in two places: the engine suite under core/ and
# the adapter/installer conformance suite under adapters/. A bare `pytest` runs both.
testpaths = core adapters
