# GitHub Copilot adapter (future work — the second real adapter)

> **STUB — see `/docs/framework-standalone-plan.md` §4.2.**

The first non-Claude adapter and the real test that `core/` is provider-agnostic:
`.github/prompts/` for the seven entry-points, `.github/agents/` for the five role
definitions, no permission allow-list. Explicitly sequenced **after** SegQC
becomes a consumer — not part of the initial extraction.
